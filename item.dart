class Item {
  String storeName;
  String itemName;
  String item;
  String itemType;
  DateTime purchaseDate;
  DateTime expiryDate;
  String status;
  int quantity;

  Item(this.storeName, this.itemName, this.item, this.itemType,
      this.purchaseDate, this.expiryDate, this.status, this.quantity);
}
